// function add(x, y) {
//     return x + y;
// }

// The function code is put into the variable add
// This allows it to be passed throughout as an argument, a return value, etc.
const add = function (x, y) {
    return x + y;
}

console.log(add(5, 5));